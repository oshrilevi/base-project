define(function() {
    var core = {

        // CHECKS IF A GIVEN OBJECT OR VARIABLE IS DEFINED.
        defined: function(x){
            return typeof x != 'undefined';
        }

    };
    return core;
});